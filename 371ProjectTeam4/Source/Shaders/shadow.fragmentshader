#version 330 core
// Ouput data
layout(location = 0) out float fragmentdepth;
 
void main(){
   //fragmentdepth = vec4(gl_FragCoord.z, gl_FragCoord.z*gl_FragCoord.z, 0.0, 0.0);   
   fragmentdepth = gl_FragCoord.z;
}